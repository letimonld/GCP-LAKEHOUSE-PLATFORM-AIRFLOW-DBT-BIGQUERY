version: 2

sources:
  - name: silver_staging__person
    database: from-warehouse-to-lakehouse
    schema: silver_staging__person
    tables:
      - name: business_entity_contact
        description: "Cross-reference table mapping stores and vendors to people to contact"
        columns:
          - name: business_entity_id
            description: "Primary key. Foreign key to BusinessEntity.BusinessEntityID"
          - name: person_id
            description: "Primary key. Foreign key to Person.BusinessEntityID"
          - name: contact_type_id
            description: "Primary key. Foreign key to ContactType.ContactTypeID"
          - name: row_guid
            description: "ROWGUIDCOL number uniquely identifying the record. Used to support a merge replication sample"
          - name: modified_date
            description: "Date and time the record was last updated"
          - name: _extract_date_
            description: "Timestamp when data was extracted from source"
          - name: date
            description: "Partition date for the record"